<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:label="@string/app_name"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

            <!-- Custom scheme used by the iOS picker page and for direct testing,
                 e.g. bankademo://pay?token=xyz -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="${urlScheme}" android:host="pay"/>
            </intent-filter>

            <!-- Universal-link-style https intent-filter. Deliberately NOT using
                 android:autoVerify="true": Android App Link verification and the
                 disambiguation chooser are mutually exclusive by design — a
                 *verified* link never prompts, it deterministically resolves to
                 one app (we confirmed on-device it silently picks whichever app
                 was most recently installed/updated, no dialog at all). Since
                 all three bank apps register this same filter unverified,
                 Android has no way to award sole ownership, so it defers to the
                 browser first — see pay.html's "Continue to pay" button below
                 for how the real chooser gets triggered from there. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data
                    android:scheme="https"
                    android:host="${deepLinkHost}"
                    android:pathPrefix="/pay"/>
            </intent-filter>

            <!-- Shared scheme all three bank apps register (unlike the
                 https:// filter above, no browser can ever claim this).
                 pay.html's Android intent:// redirect targets this instead of
                 https, since Chrome refuses to navigate an intent:// link
                 whose target could resolve back to a browser — it silently
                 no-ops rather than showing the chooser, to avoid looping
                 back into itself. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="sgqrpay" android:host="pay"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
